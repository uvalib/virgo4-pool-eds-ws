# aliases we like
alias ll='ls -al'
alias bdf='df -k'

# prompt
export HOST=$(hostname -s)
export PWD=`pwd`
export PS1='(pool-eds-ws:$HOST) $PWD$ '

# command editor
set -o vi

# path to include jruby stuff
export PATH=$PATH:/opt/jruby/bin:/usr/share/jruby/bin
